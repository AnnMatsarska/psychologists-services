import { Navigation } from 'components/Navigation/Navigation';

export const AppBar = () => {
  return (
    <header>
      <container>
        <Navigation />
      </container>
    </header>
  );
};
